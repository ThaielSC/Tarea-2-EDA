#include <string>
#include <vector>

// Partition function for Quick Sort
static int partition(const std::vector<std::string>& A, std::vector<int>& indices, int low, int high) {
    return {};

}
// Main Quick Sort function
std::vector<int> quickSort(const std::vector<std::string>& A) {
   return {};
}
